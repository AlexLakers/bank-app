apiVersion: v1
kind: Service
metadata:
  name: logstash-ls
  namespace: test
spec:
  selector:
    common.k8s.elastic.co/type: logstash
    logstash.k8s.elastic.co/name: logstash
  ports:
    - name: beats
      port: 5044
      targetPort: 5044
